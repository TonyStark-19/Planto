// type definition for review card
export type Review = {
    name: string;
    avatar: string;
    rating: number;
    comment: string;
};

// type definition for plant box
export type Plant = {
    name: string;
    tag: string;
    image: string;
    alt: string;
};